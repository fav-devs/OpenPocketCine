[package]
name = "opc-relay"
description = "Watcher-side relay client: discovery, transport, and the join session."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
opc-core-sys = { path = "../opc-core-sys" }
mdns-sd = "0.21"

[lints]
workspace = true
