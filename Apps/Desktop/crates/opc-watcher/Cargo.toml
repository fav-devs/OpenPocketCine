[package]
name = "opc-watcher"
description = "Command-line OpenPocketCine watcher: find a shared feed, join it, receive its picture."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
opc-relay = { path = "../opc-relay" }

[lints]
workspace = true
